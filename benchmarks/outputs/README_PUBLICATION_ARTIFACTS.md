# Publication Artifacts (Canonical)

Current publication-ready outputs are in:

- `s41598_final_paper_plus_ours/`
  - Figures: `fig1` to `fig24` (PNG and SVG)
  - Ablation visuals:
    - `fig_adaptivesched_ablation_progression.(png|svg)`
    - `fig_adaptivesched_ablation_dashboard.(png|svg)`
  - Tables:
    - `table5` to `table12` CSV
    - `table_adaptivesched_ablation_progression.csv`
    - `table_adaptivesched_ablation_progression_detailed.csv`
    - `table_fairness_comparison.csv`

## Notes

- This repository has been cleaned to keep only current results and active scripts.
- Any previous exploratory output folders were intentionally removed.

